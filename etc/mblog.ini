[DEFAULT]
name = mblog

[app:main]
paste.app_factory = mblog.server:launch

[server:main]
use = egg:gunicorn#main
host = 0.0.0.0
port = 80
workers = 1
worker-class = gthread
threads = 20
timeout = 30
backlog = 2048
keepalive = 2
proc_name = mblog
loglevel = DEBUG
reload=true
